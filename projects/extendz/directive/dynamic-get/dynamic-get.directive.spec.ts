import { DynamicGetDirective } from './dynamic-get.directive';

describe('DynamicGetDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicGetDirective();
    expect(directive).toBeTruthy();
  });
});
